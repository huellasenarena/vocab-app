credentials.json
token.json
apps_script/.clasp.json

# Le code du Worker est versionné, mais ses secrets/artefacts restent exclus
# (le .gitignore interne dark-brook-87cc/.gitignore couvre .dev.vars*, node_modules/, .wrangler/)
dark-brook-87cc/.DS_Store

# Artefacts Python (kindle_import)
__pycache__/
*.pyc

# Jeton perso /add + clé OpenAI dédiée + cache de sélection (kindle_import)
.token
.openai_key
.kindle_cache.json
